<%= form_for(truck) do |f| %>
  <% if truck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(truck.errors.count, "error") %> prohibited this truck from being saved:</h2>

      <ul>
      <% truck.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :plate %>
    <%= f.text_field :plate ,required: true %>
  </div>

  <div class="field">
    <%= f.label :mark %>
    <%= f.text_field :mark ,required: true %>
  </div>

  <div class="actions">
    <%= f.submit value: "Criar Caminhão" %>
  </div>
<% end %>
