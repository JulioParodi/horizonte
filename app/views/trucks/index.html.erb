<p id="notice"><%= notice %></p>

<h1>Caminhões</h1>

<table class="table">
  <thead>
    <tr>
      <th>Placa</th>
      <th>Marca</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trucks.each do |truck| %>
      <tr>
        <td><%= truck.plate %></td>
        <td><%= truck.mark %></td>
        <td><%= link_to 'Show', truck %></td>
        <td><%= link_to 'Edit', edit_truck_path(truck) %></td>
        <td><%= link_to 'Destroy', truck, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Fretes', freights_path(truck.id) %> </td>
        <td><%= link_to 'Novo frete', new_freight_url(truck.id, truck.plate) %> </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Truck', new_truck_path %>
